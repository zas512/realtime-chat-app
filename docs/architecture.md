# Architecture Overview

Monorepo with separate `client/` (Next.js) and `server/` (Express) applications and a `shared/` folder for shared types and contracts.

See each app README for local dev instructions.
