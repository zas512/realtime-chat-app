# Chat App Monorepo

Full-stack monolithic repository containing separate `client/` (Next.js) and `server/` (Node.js + Express) applications and a `shared/` folder for types and contracts.

Run the apps:

```bash
npm install
npm run dev
```

See `docs/` for architecture notes.
